# 变更说明 —— display_ecu_f429 优先级修复 + 真 AB 分区改造

> 生成时间：2026-07-02
> 基线：已应用 `change_review_fix_step1.md`（修复 1–13）+ `change_motor_can_step2.md`（F1–F8）+ `review_2026-07-02.md` 评审结论后的工程状态。
> 工具链：Keil MDK-ARM (UV4) / **armcc V5.06 update 6 (build 750)** / SPL + FreeRTOS V11.3.0
> MCU：STM32F429IGTx（1MB Flash / 128KB SRAM + 64KB CCM）
> 分支：`feature/motor_can`

---

## 0. 本文档用途（给另一台电脑上的 AI 看）

把 `display_ecu_f429` 从**基线状态**改造为**最终状态**（优先级全修复 + 真 AB 分区）。另一台电脑上的 AI 拿到本文档 + 基线工程副本，逐条应用后应得到**完全一致**的工程，并通过第 5 节的三目标编译校验。

每条改动给出：
- **文件**：相对工程根路径。
- **定位/方式**：锚点，或"整文件替换"。
- **修改前 / 修改后**：原始片段与新片段（保持原文件缩进字符——本项目部分文件 Tab、部分空格）。
- **类别**：`[逻辑]` `[配置]` `[可靠]` `[链接]` `[新建]`。
- **原因**：为什么改。

> ⚠️ 本项目存在**透明加密**：源机 `.c/.h/二进制` 可读，脱离源机变乱码；**仅 `.md` 可跨机器阅读**。故变更以本文档为准、不依赖 git/diff；被整体重写的文件给出**完整新内容**。

> 改动分两阶段（均独立编译通过）：**阶段 A = 优先级修复**；**阶段 B = 真 AB 改造**。被两阶段都触及的文件（`ota.c`/`boot_main.c`/`boot_decision.c`），其**最终状态**以阶段 B 为准；阶段 A 的中间形态仅供理解演进。

---

## 1. 改动总览

### 1.1 被修改的文件（12 个）+ 新建文件（1 个）

> **补充改动（2026-07-02 追加，已并入下列各节）**：
> ① **B 镜像并进 `app.uvprojx` 作为第二 Target**（取消独立的 `app_b.uvprojx`）——新增 `.c/.h` 只在一份工程里维护，`UV4 -t 目标名` 选目标构建。
> ② **`ota.c` 增"镜像链接地址防错包"校验**——YMODEM 写完后校验 reset-handler 地址落在目标槽范围内，发错 bin 直接拒绝、不污染活跃槽。
```
阶段 A（优先级修复）：
  bootloader/ota_params.c     H1 参数区 append-only 日志 + 记录 CRC；L5 CRC 表 3 处笔误修正
  bootloader/boot_decision.c  M1 CRC 失败重启重试（阶段 B 再整体重写为真 AB）
  bootloader/ota.c            H2 备份拆分 + M2 版本解析（阶段 B 再整体重写为真 AB + 链接地址防错包）
  bootloader/ota.h            H2 增 backup 声明（阶段 B 改回单入口）
  bootloader/boot_main.c      H2 备份调用一次（阶段 B 移除该调用）
  app/bsp_can.c               M5 使能 ABOM 自动 Bus-Off 恢复
  task/task_query.c           M3+M4 复用 boot_config.h、按 SCB->VTOR 自证运行槽
  task/mod_comm_can.h         L1 include 大小写修正
  app/bsp_key.c               L4 信号量句柄判空
  mdk/app.uvprojx             L2 OutputName=app、IncludePath 加 ..\bootloader；阶段 B 追加第二 Target
  mdk/boot.uvprojx            L2 OutputName=boot
阶段 B（真 AB 改造）：
  firmware/cmsis/device/system_stm32f4xx.c   VECT_TAB_OFFSET 增加 APP_SLOT_B 分支
  bootloader/boot_decision.c                 按 active_partition 选址 + 切槽回滚（整体重写）
  bootloader/ota.c                           写非活跃槽 + 翻转 active + 链接地址防错包（整体重写）
  bootloader/ota.h                           单入口
  bootloader/boot_main.c                     移除备份调用
  mdk/app.uvprojx                            追加 Target "stm32f429_b"（B 槽，链接 0x08080000）
  mdk/app_b.sct                  [新建] B 槽 scatter，链接 0x08080000
```

### 1.2 编译验证结论（最终状态）
| 目标 | 命令 | 结果 | 产物 |
| --- | --- | --- | --- |
| `mdk/boot.uvprojx` | `UV4 -r boot.uvprojx` | **0E 0W** | `Objects\boot.axf` |
| `mdk/app.uvprojx` · Target `stm32f429` | `UV4 -r app.uvprojx -t stm32f429` | **0E 1W** | `Objects\app.axf` + `app.bin`（A 槽，0x08020000）|
| `mdk/app.uvprojx` · Target `stm32f429_b` | `UV4 -r app.uvprojx -t stm32f429_b` | **0E 1W** | `Objects\app_b.axf` + `app_b.bin`（B 槽，0x08080000）|

唯一警告恒为 `third_lib/FreeRTOS/src/port.c(836): #550-D "ucCurrentPriority" was set but never used`（FreeRTOS 库本身，与本次改动无关）。

map 实测：`app.map` 的 `Execution Region ER_IROM1 (Exec base 0x08020000)`；`app_b.map` 的 `Execution Region ER_IROM1 (Exec base 0x08080000)`。两镜像槽地址正确。

---

## 2. 阶段 A —— 优先级修复（逐条）

> 下列 `[可靠] [逻辑] [配置] [质量]` 为类别。被阶段 B 再次重写的文件，本节给出阶段 A 的改动用于理解；最终落地以第 3 节为准。

### A-H1 / A-L5 —— `bootloader/ota_params.c`：整体重写 `[可靠]`
**整文件替换为第 4.1 节给出的完整内容。**

要点：
- Sector 4（64KB）改为 **append-only 磨损均衡日志**：每槽 64B、共 1024 槽；`save()` 顺序追加写下一个已擦除槽（无需每次擦除），仅当日志满（每 1024 次 save）才整扇区擦除并重写槽 0。
- 每条记录 = `magic(4) + seq(4) + ota_param_t(52) + crc(4) = 64B`，CRC 覆盖前 60 字节。写入中途掉电 → 该记录 CRC 不匹配 → `load()` 跳过取上一条有效记录。常规 save 完全掉电安全。
- **L5**：原 CRC32 表有 3 处笔误（`idx 0x4A: 0x9609A88F→0x9609A88E`、`idx 0x5B: 0x0FCB988C→0xFCB9887C`、`idx 0xBB: 0x5CB30A04→0x5CB36A04`），导致 bootloader CRC 偏离标准（虽存/验同表自洽、OTA 未暴露，但测试向量 `CRC32("123456789")=0xCBF43926` 系侥幸通过）。已全量比对修正为标准 CRC-32（IEEE 802.3）。
- 外部 API `ota_params_init/load/save` 与 `ota_param_t` 结构**不变**，调用方无需改。

> 说明：因日志格式变更，旧参数区（旧单结构体格式）首次启动会被判为"无有效记录"而 `ota_params_init()` 重置（active=A、各 size/crc=0）。IDLE 路径只做 SP 校验，出厂 App A 仍可正常启动，无损可用性。

### A-H2 —— 备份只做一次（**阶段 B 被"写非活跃槽、无需备份"取代**） `[可靠]`
阶段 A 在 `boot_main.c` 进入 OTA 前调用一次 `ota_backup_active_to_b()`、`ota.c` 把备份从 `ota_ymodem_start` 拆出。阶段 B 改为真 AB 后，活跃槽即天然回滚源、**不再需要 A→B 备份**，该调用与函数整体移除（见第 3 节）。最终状态无备份动作。

### A-M1 —— `bootloader/boot_decision.c`：CRC 失败真正重启重试 `[逻辑]`
**定位**：COMPLETE 分支 CRC 失败且未超限处。（阶段 B 整体重写，本条语义保留并增强为切槽回滚。）

修改前：
```c
            // 未超限，等下次重启再试
            printf("[BOOT] CRC mismatch (attempt %u), retry next boot.\r\n",
                   g_ota_param.boot_count);
            return 0;
```
修改后（阶段 A 形态；阶段 B 见 3.3）：
```c
            // 未超限：重启重试（boot_count 已持久化，重启后递增；超限则回滚）
            printf("[BOOT] CRC mismatch (attempt %u/%u), reboot to retry...\r\n",
                   g_ota_param.boot_count, g_ota_param.max_boot_count);
            NVIC_SystemReset();
            return 0;   /* 不可达，保险 */
```
**原因**：原 `return 0` 实际 fall-through 进 YMODEM，与注释"retry next boot"不符；改 `NVIC_SystemReset()` 真正重启重试，激活 boot_count 回滚机制。`boot_count` 在本分支之前已 `ota_params_save` 持久化。

### A-M2 —— 版本号取自 YMODEM 文件名（**阶段 B 整体重写时保留该解析**） `[逻辑]`
新增 `parse_version_from_name()`：从文件名解析 `v<maj>_<min>` / `v<maj>.<min>`（v 不分大小写）→ `(maj<<16)|min`；失败返回 0。完整实现见第 4.3 节 `ota.c`。**原因**：原 `app_a_version = 0x00010001` 硬编码，版本号永不反映真实版本。

### A-M3 / A-M4 —— `task/task_query.c`：整体重写 `[逻辑/质量]`
**整文件替换为第 4.2 节给出的完整内容。**

- 删除本地重定义的 `ota_param_snap_t` 与 `FLASH_BASE_ADDR/APP_A_ADDR/APP_B_ADDR/OTA_PARAM_ADDR/APP_A_ACTIVE/APP_B_ACTIVE`。
- `#include "boot_config.h"`（地址常量单一来源；需 app 工程包含路径加 `..\bootloader`，见 A-L2）。
- 运行槽位**由 `SCB->VTOR` 自证**（A 链接于 0x08020000、B 链接于 0x08080000，VTOR 即运行基址）：`active_app = SCB->VTOR; partition = (active_app==APP_A_ADDR)?1:2;`。不再读 OTA 参数区，M4（active=B 死分支）自然消除：真 AB 下 App 真在 B 运行时即报 partition=2。

### A-M5 —— `app/bsp_can.c`：使能 ABOM `[配置]`
**定位**：`BSP_CAN_Init()` 的 CAN 结构体配置段。

修改前：
```c
    CAN_StructInit(&CAN_InitStructure);
    CAN_InitStructure.CAN_Prescaler = 9;
    CAN_InitStructure.CAN_BS1 = CAN_BS1_7tq;
    CAN_InitStructure.CAN_BS2 = CAN_BS2_2tq;
    CAN_InitStructure.CAN_SJW = CAN_SJW_1tq;
    CAN_InitStructure.CAN_Mode = CAN_Mode_Normal;
```
修改后：
```c
    CAN_StructInit(&CAN_InitStructure);
    CAN_InitStructure.CAN_Prescaler = 9;
    CAN_InitStructure.CAN_BS1 = CAN_BS1_7tq;
    CAN_InitStructure.CAN_BS2 = CAN_BS2_2tq;
    CAN_InitStructure.CAN_SJW = CAN_SJW_1tq;
    CAN_InitStructure.CAN_Mode = CAN_Mode_Normal;
    CAN_InitStructure.CAN_ABOM = ENABLE;   /* M5: 总线异常 Bus-Off 后自动恢复，无需软件重入 */
```
**原因**：原未置 ABOM，总线异常进入 Bus-Off 后静默停发、需软件 `CAN_Init` 重入；车载场景应开自动恢复。

### A-L1 —— `task/mod_comm_can.h`：include 大小写修正 `[质量]`
修改前：`#include "CAN_Protocol.h"`
修改后：`#include "can_protocol.h"`
**原因**：实际文件名小写 `can_protocol.h`；Windows 大小写不敏感能编过，迁 Linux/大小写敏感构建即断。

### A-L2 —— `mdk/app.uvprojx` + `mdk/boot.uvprojx`：输出名分离 + 包含路径 `[配置]`
**app.uvprojx** 两处：
1. `<OutputName>stm32f429</OutputName>` → `<OutputName>app</OutputName>`
2. `<IncludePath>..\app;..\driver;..\firmware\cmsis\core;..\firmware\cmsis\device;..\firmware\driver\inc;..\components;..\third_lib\FreeRTOS;..\third_lib\FreeRTOS\inc;..\task;..\protocol</IncludePath>` → 末尾追加 `;..\bootloader`

**boot.uvprojx** 一处：`<OutputName>stm32f429</OutputName>` → `<OutputName>boot</OutputName>`

**原因**：原两工程输出同名 `stm32f429.axf` 同目录互相覆盖；分离为 `app`/`boot` 后各自独立（post-build 的 `!L` 占位符自动跟随新名，无需改）。app 加 `..\bootloader` 供 `task_query.c` 引 `boot_config.h`。

### A-L4 —— `app/bsp_key.c`：信号量句柄判空 `[逻辑]`
**定位**：`prvKeyScanTask()` 给信号量处。

修改前：
```c
		if (sCurrKey1State == Bit_SET && sPrevKey1State == Bit_RESET) {
				xSemaphoreGive(xKey1Sem);
		}
		if (sCurrKey2State == Bit_SET && sPrevKey2State == Bit_RESET) {
				xSemaphoreGive(xKey2Sem);
		}
```
修改后：
```c
		if (sCurrKey1State == Bit_SET && sPrevKey1State == Bit_RESET) {
				if (xKey1Sem != NULL) xSemaphoreGive(xKey1Sem);   /* L4: 句柄存在才 give */
		}
		if (sCurrKey2State == Bit_SET && sPrevKey2State == Bit_RESET) {
				if (xKey2Sem != NULL) xSemaphoreGive(xKey2Sem);   /* L4: 句柄存在才 give */
		}
```
（行首为 2 个 Tab，逐字保持。）**原因**：若 `xSemaphoreCreateBinary` 失败（句柄 NULL），`xSemaphoreGive(NULL)` 行为未定义。

---

## 3. 阶段 B —— 真 AB 分区改造（方案①，逐条）

### 3.1 `firmware/cmsis/device/system_stm32f4xx.c`：VECT_TAB_OFFSET 增加 APP_SLOT_B 分支 `[配置]`
**定位**：文件中 VECT_TAB_OFFSET 的 `#ifdef BOOTLOADER` 段（约 358 行）。

修改前：
```c
#ifdef BOOTLOADER
/* Bootloader: 向量表位于 Flash 基址 0x08000000 */
#define VECT_TAB_OFFSET  0x00
#else
/* App: 向量表偏移到 0x08020000 */
#define VECT_TAB_OFFSET  0x00020000
#endif
```
修改后：
```c
#ifdef BOOTLOADER
/* Bootloader: 向量表位于 Flash 基址 0x08000000 */
#define VECT_TAB_OFFSET  0x00
#elif defined(APP_SLOT_B)
/* App B: 向量表偏移到 0x08080000（真 AB 的 B 槽镜像）*/
#define VECT_TAB_OFFSET  0x00080000
#else
/* App A: 向量表偏移到 0x08020000 */
#define VECT_TAB_OFFSET  0x00020000
#endif
```
**原因**：B 镜像需在 SystemInit 即把 VTOR 指向 0x08080000（虽 App `main()` 会再设 `SCB->VTOR=&__Vectors`，且 boot→main 期间中断关闭、不会被使用，但提前正确可消除一切边界疑虑）。B 目标工程定义 `APP_SLOT_B` 宏（见 3.6）。

### 3.2 `mdk/app_b.sct`：[新建] B 槽 scatter `[新建/链接]`
**新建文件 `mdk/app_b.sct`，内容为第 4.4 节完整给出。** 与 `app.sct` 仅加载/执行区地址不同（`0x08080000`），RAM 布局相同（A/B 任一时刻只运行一份）。

### 3.3 `bootloader/boot_decision.c`：整体重写（按 active 选址 + 切槽回滚） `[逻辑/链接]`
**整文件替换为第 4.5 节给出的完整内容。**

要点：
- `get_active_addr()` 按 `active_partition` 返回 `APP_A_ADDR`/`APP_B_ADDR`。
- COMPLETE 分支 CRC 校验**当前活跃槽**（用其元数据 size/crc）：通过→IDLE 跳转；失败未超限→`NVIC_SystemReset()` 重启重试；超限→`rollback_to_other()` **切回另一槽**（无需 Flash 搬运），另一槽 CRC 通过则切换 active 并跳转。
- `rollback_to_other()`：另一槽有元数据则严格 CRC 校验；无元数据（如出厂槽）则仅凭 SP 合法性信任。

### 3.4 `bootloader/ota.c`：整体重写（写非活跃槽 + 翻转 active） `[逻辑/链接]`
**整文件替换为第 4.3 节给出的完整内容。**

要点：
- 目标 = 当前**非活跃槽**（active 为 A 则写 B，反之写 A），YMODEM 直接写入，不影响正在运行的活跃槽。
- 成功后：记录新槽元数据（version/size/crc，旧槽元数据保持作回滚依据）→ 翻转 `active_partition` 到新槽 → `ota_state=COMPLETE`、`boot_count=0` → `NVIC_SystemReset()`。
- 不再做 A→B 备份（真 AB 下活跃槽即回滚源）。

### 3.5 `bootloader/ota.h` + `bootloader/boot_main.c` `[逻辑]`
**`ota.h` 整文件替换为第 4.6 节内容**（仅 `void ota_ymodem_start(void);`）。

**`boot_main.c` OTA 入口段**：

修改前（阶段 A 形态）：
```c
    // ===== 未能跳转或用户选择升级, 直接启动 OTA =====
    printf("[BOOT] Entering upgrade mode.\r\n");
    ota_backup_active_to_b();   /* H2: 本次 OTA 会话只备份一次，重试不重复擦写 B */
    while (1) {
        ota_ymodem_start();
        printf("[BOOT] OTA failed, retry in 3s...\r\n");
        Delay_ms(3000);
    }
```
修改后：
```c
    // ===== 未能跳转或用户选择升级, 直接启动 OTA =====
    printf("[BOOT] Entering upgrade mode.\r\n");
    while (1) {
        ota_ymodem_start();   /* 真 AB：写非活跃槽，活跃槽保持不变 */
        printf("[BOOT] OTA failed, retry in 3s...\r\n");
        Delay_ms(3000);
    }
```

### 3.6 `mdk/app.uvprojx`：追加第二 Target "stm32f429_b"（B 槽） `[链接]`
**方式**：在 `app.uvprojx` 的 `<Targets>` 内、现有 `<Target>...</Target>` 之后，**再插入一个完整的 `<Target>` 块**（= 现有 Target 块的副本 + 下表 8 处改动）。两个 Target 各自带一份 `<Groups>`（文件清单相同），编译同一份源码、仅链接/输出不同。**不再创建独立的 `app_b.uvprojx`**——后续新增 `.c/.h` 只加到这一份工程一次（Keil 多 Target 工程在 GUI 里共享组结构）。

新 Target 块相对原 A Target 块的 8 处差异（与 A 共存于同一文件）：

| # | 原值（A Target） | 新值（B Target） |
| --- | --- | --- |
| 1 | `<TargetName>stm32f429</TargetName>` | `<TargetName>stm32f429_b</TargetName>` |
| 2 | `<OutputName>app</OutputName>` | `<OutputName>app_b</OutputName>` |
| 3 | `--output app.bin !L` | `--output app_b.bin !L` |
| 4 | `<ScatterFile>.\app.sct</ScatterFile>` | `<ScatterFile>.\app_b.sct</ScatterFile>` |
| 5 | `IROM(0x08020000,0x00060000)` | `IROM(0x08080000,0x00060000)` |
| 6 | `<StartAddress>0x8020000</StartAddress>`（2 处：IROM、OCR_RVCT4） | `<StartAddress>0x8080000</StartAddress>` |
| 7 | `<Define>STM32F429_439xx,USE_STDPERIPH_DRIVER</Define>` | `<Define>STM32F429_439xx,USE_STDPERIPH_DRIVER,APP_SLOT_B</Define>` |
| 8 | `<LayName>app</LayName>` | `<LayName>app_b</LayName>` |

> 实施技巧（推荐）：先 `cp app.uvprojx app_b_tmp.uvprojx`，对副本跑下面 8 条 sed 得到"B Target 配态"，再把副本里 `<Target> ... </Target>` 整段贴回 `app.uvprojx` 的 `</Targets>` 之前，最后删副本。等价 sed（对副本）：
> ```
> sed -i 's#<TargetName>stm32f429</TargetName>#<TargetName>stm32f429_b</TargetName>#'   app_b_tmp.uvprojx
> sed -i 's#<OutputName>app</OutputName>#<OutputName>app_b</OutputName>#'               app_b_tmp.uvprojx
> sed -i 's#--output app\.bin#--output app_b.bin#'                                     app_b_tmp.uvprojx
> sed -i 's#app\.sct</ScatterFile>#app_b.sct</ScatterFile>#'                           app_b_tmp.uvprojx
> sed -i 's#IROM(0x08020000,0x00060000)#IROM(0x08080000,0x00060000)#'                  app_b_tmp.uvprojx
> sed -i 's#<StartAddress>0x8020000</StartAddress>#<StartAddress>0x8080000</StartAddress>#g' app_b_tmp.uvprojx
> sed -i 's#<Define>STM32F429_439xx,USE_STDPERIPH_DRIVER</Define>#<Define>STM32F429_439xx,USE_STDPERIPH_DRIVER,APP_SLOT_B</Define>#' app_b_tmp.uvprojx
> sed -i 's#<LayName>app</LayName>#<LayName>app_b</LayName>#'                          app_b_tmp.uvprojx
> ```
> **构建**：`UV4 -r app.uvprojx -t stm32f429`（A）与 `UV4 -r app.uvprojx -t stm32f429_b`（B）。`app.uvoptx` 无需手写——首次构建时 UV4 自动生成涵盖两个 Target 的 optx（用 `<IsCurrentTarget>` 标记活跃目标）；即便删掉 `app.uvoptx`，UV4 也会自动重建。

---

## 4. 整体重写文件的完整新内容

### 4.1 `bootloader/ota_params.c`（H1 + L5）

```c
/**
  ******************************************************************************
  * @file    ota_params.c
  * @brief   OTA 参数管理 — CRC32 校验 + 掉电安全的 append-only 磨损均衡日志
  *
  *          标准 CRC-32 (IEEE 802.3 / PKZIP): 多项式 0xEDB88320（反射），查表法
  *          标准测试向量: CRC32("123456789") = 0xCBF43926
  *
  *          存储方案（H1 修复）：
  *          Sector 4 (64KB) 作为 append-only 日志区，每条记录占 1 个 64B 槽位，
  *          共 1024 槽。save() 顺序追加写下一个已擦除槽（无需每次擦除），
  *          仅当日志区满（每 1024 次 save）才整扇区擦除并重写。
  *
  *          掉电安全：单次 save 只写一条 64B 记录，写入中途掉电 → 该记录 CRC
  *          不匹配 → load() 跳过它、取上一条有效记录。故常规 save（每启动/每次
  *          OTA 发生）完全掉电安全；仅"日志满触发整扇区擦除"这一极低频事件
  *          （1/1024）存在擦除-重写之间的窗口，且该窗口失败时退化为"参数初
  *          始化、回滚到默认 active=A"，仍可安全启动。
  ******************************************************************************
  */

#include "ota_params.h"
#include "flash_control.h"
#include <string.h>

// ===== CRC32 查表（多项式 0xEDB88320，反射；已修正 L5 的 3 处笔误为标准表） =====
static const uint32_t crc32_table[256] = {
    0x00000000, 0x77073096, 0xEE0E612C, 0x990951BA,
    0x076DC419, 0x706AF48F, 0xE963A535, 0x9E6495A3,
    0x0EDB8832, 0x79DCB8A4, 0xE0D5E91E, 0x97D2D988,
    0x09B64C2B, 0x7EB17CBD, 0xE7B82D07, 0x90BF1D91,
    0x1DB71064, 0x6AB020F2, 0xF3B97148, 0x84BE41DE,
    0x1ADAD47D, 0x6DDDE4EB, 0xF4D4B551, 0x83D385C7,
    0x136C9856, 0x646BA8C0, 0xFD62F97A, 0x8A65C9EC,
    0x14015C4F, 0x63066CD9, 0xFA0F3D63, 0x8D080DF5,
    0x3B6E20C8, 0x4C69105E, 0xD56041E4, 0xA2677172,
    0x3C03E4D1, 0x4B04D447, 0xD20D85FD, 0xA50AB56B,
    0x35B5A8FA, 0x42B2986C, 0xDBBBC9D6, 0xACBCF940,
    0x32D86CE3, 0x45DF5C75, 0xDCD60DCF, 0xABD13D59,
    0x26D930AC, 0x51DE003A, 0xC8D75180, 0xBFD06116,
    0x21B4F4B5, 0x56B3C423, 0xCFBA9599, 0xB8BDA50F,
    0x2802B89E, 0x5F058808, 0xC60CD9B2, 0xB10BE924,
    0x2F6F7C87, 0x58684C11, 0xC1611DAB, 0xB6662D3D,
    0x76DC4190, 0x01DB7106, 0x98D220BC, 0xEFD5102A,
    0x71B18589, 0x06B6B51F, 0x9FBFE4A5, 0xE8B8D433,
    0x7807C9A2, 0x0F00F934, 0x9609A88E, 0xE10E9818,
    0x7F6A0DBB, 0x086D3D2D, 0x91646C97, 0xE6635C01,
    0x6B6B51F4, 0x1C6C6162, 0x856530D8, 0xF262004E,
    0x6C0695ED, 0x1B01A57B, 0x8208F4C1, 0xF50FC457,
    0x65B0D9C6, 0x12B7E950, 0x8BBEB8EA, 0x00FCB988,
    0x62DD1DDF, 0x15DA2D49, 0x8CD37CF3, 0xFBD44C65,
    0x4DB26158, 0x3AB551CE, 0xA3BC0074, 0xD4BB30E2,
    0x4ADFA541, 0x3DD895D7, 0xA4D1C46D, 0xD3D6F4FB,
    0x4369E96A, 0x346ED9FC, 0xAD678846, 0xDA60B8D0,
    0x44042D73, 0x33031DE5, 0xAA0A4C5F, 0xDD0D7CC9,
    0x5005713C, 0x270241AA, 0xBE0B1010, 0xC90C2086,
    0x5768B525, 0x206F85B3, 0xB966D409, 0xCE61E49F,
    0x5EDEF90E, 0x29D9C998, 0xB0D09822, 0xC7D7A8B4,
    0x59B33D17, 0x2EB40D81, 0xB7BD5C3B, 0xC0BA6CAD,
    0xEDB88320, 0x9ABFB3B6, 0x03B6E20C, 0x74B1D29A,
    0xEAD54739, 0x9DD277AF, 0x04DB2615, 0x73DC1683,
    0xE3630B12, 0x94643B84, 0x0D6D6A3E, 0x7A6A5AA8,
    0xE40ECF0B, 0x9309FF9D, 0x0A00AE27, 0x7D079EB1,
    0xF00F9344, 0x8708A3D2, 0x1E01F268, 0x6906C2FE,
    0xF762575D, 0x806567CB, 0x196C3671, 0x6E6B06E7,
    0xFED41B76, 0x89D32BE0, 0x10DA7A5A, 0x67DD4ACC,
    0xF9B9DF6F, 0x8EBEEFF9, 0x17B7BE43, 0x60B08ED5,
    0xD6D6A3E8, 0xA1D1937E, 0x38D8C2C4, 0x4FDFF252,
    0xD1BB67F1, 0xA6BC5767, 0x3FB506DD, 0x48B2364B,
    0xD80D2BDA, 0xAF0A1B4C, 0x36034AF6, 0x41047A60,
    0xDF60EFC3, 0xA867DF55, 0x316E8EEF, 0x4669BE79,
    0xCB61B38C, 0xBC66831A, 0x256FD2A0, 0x5268E236,
    0xCC0C7795, 0xBB0B4703, 0x220216B9, 0x5505262F,
    0xC5BA3BBE, 0xB2BD0B28, 0x2BB45A92, 0x5CB36A04,
    0xC2D7FFA7, 0xB5D0CF31, 0x2CD99E8B, 0x5BDEAE1D,
    0x9B64C2B0, 0xEC63F226, 0x756AA39C, 0x026D930A,
    0x9C0906A9, 0xEB0E363F, 0x72076785, 0x05005713,
    0x95BF4A82, 0xE2B87A14, 0x7BB12BAE, 0x0CB61B38,
    0x92D28E9B, 0xE5D5BE0D, 0x7CDCEFB7, 0x0BDBDF21,
    0x86D3D2D4, 0xF1D4E242, 0x68DDB3F8, 0x1FDA836E,
    0x81BE16CD, 0xF6B9265B, 0x6FB077E1, 0x18B74777,
    0x88085AE6, 0xFF0F6A70, 0x66063BCA, 0x11010B5C,
    0x8F659EFF, 0xF862AE69, 0x616BFFD3, 0x166CCF45,
    0xA00AE278, 0xD70DD2EE, 0x4E048354, 0x3903B3C2,
    0xA7672661, 0xD06016F7, 0x4969474D, 0x3E6E77DB,
    0xAED16A4A, 0xD9D65ADC, 0x40DF0B66, 0x37D83BF0,
    0xA9BCAE53, 0xDEBB9EC5, 0x47B2CF7F, 0x30B5FFE9,
    0xBDBDF21C, 0xCABAC28A, 0x53B39330, 0x24B4A3A6,
    0xBAD03605, 0xCDD70693, 0x54DE5729, 0x23D967BF,
    0xB3667A2E, 0xC4614AB8, 0x5D681B02, 0x2A6F2B94,
    0xB40BBE37, 0xC30C8EA1, 0x5A05DF1B, 0x2D02EF8D,
};

// ===== 计算缓冲区的 CRC32 =====
uint32_t crc32_calc(const uint8_t *data, uint32_t len)
{
    uint32_t crc = 0xFFFFFFFF;
    for (uint32_t i = 0; i < len; i++) {
        crc = crc32_table[(crc ^ data[i]) & 0xFF] ^ (crc >> 8);
    }
    return crc ^ 0xFFFFFFFF;
}

// ===== 计算 Flash 中数据的 CRC32 =====
uint32_t crc32_flash(uint32_t addr, uint32_t len)
{
    uint32_t crc = 0xFFFFFFFF;
    uint8_t *p = (uint8_t *)addr;
    for (uint32_t i = 0; i < len; i++) {
        crc = crc32_table[(crc ^ p[i]) & 0xFF] ^ (crc >> 8);
    }
    return crc ^ 0xFFFFFFFF;
}

// ============================================================
// append-only 日志布局（Sector 4, 64KB）
//   每槽 64B，共 1024 槽；每条记录 = magic(4) + seq(4) + ota_param_t(52) + crc(4) = 64B
// ============================================================
#define OTA_LOG_ADDR        OTA_PARAM_ADDR            // 0x08010000
#define OTA_LOG_SIZE        0x00010000U               // 64KB（整扇区）
#define OTA_SLOT_SIZE       64U
#define OTA_SLOT_COUNT      (OTA_LOG_SIZE / OTA_SLOT_SIZE)   // 1024

typedef struct {
    uint32_t      magic;     // OTA_MAGIC
    uint32_t      seq;       // 单调递增序号，用于选最新
    ota_param_t   param;     // 载荷（52B，packed）
    uint32_t      crc;       // crc32 覆盖 magic+seq+param（即本字段之前的全部字节）
} ota_log_record_t;          // = 64B（无填充：param@8 起 4 字节对齐，crc@60 起 4 字节对齐）

#define RECORD_CRC_LEN      (sizeof(ota_log_record_t) - sizeof(uint32_t))   // 60

// ===== 内部：纯读取最新有效记录（不触发 save，避免递归） =====
static int load_latest(ota_param_t *param, uint32_t *out_seq)
{
    uint32_t best_seq = 0;
    int found = 0;

    memset(param, 0, sizeof(ota_param_t));

    for (uint32_t i = 0; i < OTA_SLOT_COUNT; i++) {
        const volatile ota_log_record_t *rec =
            (const volatile ota_log_record_t *)(OTA_LOG_ADDR + i * OTA_SLOT_SIZE);

        // 首个非 magic 槽视为日志尾（append-only，无空洞）
        if (rec->magic != OTA_MAGIC) {
            break;
        }

        // CRC 不匹配 = 写入中途掉电的损坏记录，跳过
        uint32_t crc = crc32_calc((const uint8_t *)rec, RECORD_CRC_LEN);
        if (crc != rec->crc) {
            continue;
        }

        if (!found || rec->seq >= best_seq) {
            best_seq = rec->seq;
            *param = rec->param;
            found = 1;
        }
    }

    if (out_seq) *out_seq = found ? best_seq : 0;
    return found;
}

// ===== 构造一条记录并写入指定槽地址（槽必须已擦除） =====
static int write_record(uint32_t addr, const ota_param_t *param, uint32_t seq)
{
    ota_log_record_t rec;
    rec.magic = OTA_MAGIC;
    rec.seq   = seq;
    rec.param = *param;
    rec.crc   = crc32_calc((const uint8_t *)&rec, RECORD_CRC_LEN);

    return flash_if_write(addr, (const uint8_t *)&rec, sizeof(ota_log_record_t));
}

// ===== 参数管理函数 =====

// 初始化参数区（首次上电或无有效记录时调用）：擦除 + 写默认记录到槽 0
int ota_params_init(void)
{
    ota_param_t param;
    memset(&param, 0, sizeof(param));

    param.magic            = OTA_MAGIC;
    param.param_version    = 0x00010000;        // v1.0.0
    param.active_partition = APP_A_ACTIVE;       // 默认运行 App A
    param.ota_state        = OTA_STATE_IDLE;
    param.boot_count       = 0;
    param.max_boot_count   = MAX_BOOT_ATTEMPTS;  // 默认 3 次

    param.app_a_version    = 0;
    param.app_a_size       = 0;
    param.app_a_crc32      = 0;
    param.app_b_version    = 0;
    param.app_b_size       = 0;
    param.app_b_crc32      = 0;

    flash_if_init();
    if (flash_if_erase(OTA_LOG_ADDR, OTA_LOG_SIZE) != 0) {
        flash_if_lock();
        return -1;
    }
    if (write_record(OTA_LOG_ADDR, &param, 0) != 0) {
        flash_if_lock();
        return -1;
    }
    flash_if_lock();
    return 0;
}

// 从 Flash 加载最新有效参数到 RAM
int ota_params_load(ota_param_t *param)
{
    uint32_t seq = 0;
    int found = load_latest(param, &seq);

    // 防御：历史遗留 max_boot_count==0 修正并持久化
    if (found && param->magic == OTA_MAGIC && param->max_boot_count == 0) {
        param->max_boot_count = MAX_BOOT_ATTEMPTS;
        ota_params_save(param);
    }
    return found ? 0 : 0;   // 始终返回 0；调用方据 magic 判断是否有效
}

// 保存参数：追加写下一条记录；日志满则整扇区擦除并重写槽 0
int ota_params_save(const ota_param_t *param)
{
    ota_param_t cur;
    uint32_t cur_seq;
    load_latest(&cur, &cur_seq);

    uint32_t new_seq = cur_seq + 1;

    flash_if_init();

    // 寻找首个已擦除槽（magic != OTA_MAGIC）
    uint32_t free_slot = OTA_SLOT_COUNT;
    for (uint32_t i = 0; i < OTA_SLOT_COUNT; i++) {
        const volatile uint32_t *slot_magic =
            (const volatile uint32_t *)(OTA_LOG_ADDR + i * OTA_SLOT_SIZE);
        if (*slot_magic != OTA_MAGIC) {
            free_slot = i;
            break;
        }
    }

    if (free_slot < OTA_SLOT_COUNT) {
        // 追加写已擦除槽，无需擦除
        if (write_record(OTA_LOG_ADDR + free_slot * OTA_SLOT_SIZE, param, new_seq) != 0) {
            flash_if_lock();
            return -1;
        }
    } else {
        // 日志满：整扇区擦除后重写槽 0（每 1024 次 save 触发一次）
        if (flash_if_erase(OTA_LOG_ADDR, OTA_LOG_SIZE) != 0) {
            flash_if_lock();
            return -1;
        }
        if (write_record(OTA_LOG_ADDR, param, new_seq) != 0) {
            flash_if_lock();
            return -1;
        }
    }

    flash_if_lock();
    return 0;
}
```

> `bootloader/ota_params.h` **不改**（`ota_param_t` 结构、`crc32_calc/crc32_flash` 与 `ota_params_init/load/save` 声明均保持原样）。

### 4.2 `task/task_query.c`（M3+M4）

```c
/**
  ******************************************************************************
  * @file    task_query.c
  * @brief   UART Query Task — 0xAA 0x55 protocol, interrupt-driven RX
  *
  * Command: 0x01 = chip info query
  * Depends: USART1 (RXNE interrupt -> ring buffer -> UART_RxGet)
  *
  * M3/M4: 地址/分区不再本地重定义、也不再读 OTA 参数区；运行槽位由 SCB->VTOR
  *        自证（App A 链接于 0x08020000、App B 链接于 0x08080000，VTOR 即运行基址）。
  ******************************************************************************
  */
#include "task_query.h"
#include "main.h"
#include "boot_config.h"   /* M3: 地址常量单一来源（FLASH_BASE_ADDR / APP_A_ADDR / APP_B_ADDR） */
#include "usart.h"
#include "bsp_log.h"
#include <string.h>

// ======================== Protocol constants ========================
#define PKT_HEADER1             0xAA
#define PKT_HEADER2             0x55
#define CMD_CHIP_INFO           0x01
#define PKT_DATA_LEN            13

// ======================== CRC16 (poly 0x1021) ========================
static uint16_t crc16_calc(const uint8_t *data, uint8_t len)
{
    uint16_t crc = 0;
    for (uint8_t i = 0; i < len; i++) {
        crc ^= (uint16_t)data[i] << 8;
        for (uint8_t j = 0; j < 8; j++) {
            if (crc & 0x8000)
                crc = (crc << 1) ^ 0x1021;
            else
                crc <<= 1;
        }
    }
    return crc;
}

// ======================== Packet send ========================
static void send_packet(uint8_t type, const uint8_t *data, uint8_t len)
{
    uint8_t buf[32];
    uint8_t idx = 0;

    buf[idx++] = PKT_HEADER1;
    buf[idx++] = PKT_HEADER2;
    buf[idx++] = type;
    buf[idx++] = len;
    memcpy(&buf[idx], data, len);
    idx += len;

    uint16_t crc = crc16_calc(data, len);
    buf[idx++] = (uint8_t)(crc >> 8);
    buf[idx++] = (uint8_t)(crc & 0xFF);

    taskENTER_CRITICAL();
    UART_SendArray(buf, idx);
    taskEXIT_CRITICAL();
}

// ======================== Command 0x01: Chip info query ========================
static void handle_chip_info_query(void)
{
    uint32_t boot_addr  = FLASH_BASE_ADDR;
    uint32_t active_app = SCB->VTOR;                       /* 当前运行镜像基址 */
    uint8_t  partition  = (active_app == APP_A_ADDR) ? 1 : 2;  /* M4: A=1 / B=2，自证 */

    uint8_t data[PKT_DATA_LEN];
    data[0] = 0xF4;
    data[1] = partition;
    data[2] = (uint8_t)(boot_addr);
    data[3] = (uint8_t)(boot_addr >> 8);
    data[4] = (uint8_t)(boot_addr >> 16);
    data[5] = (uint8_t)(boot_addr >> 24);
    data[6] = (uint8_t)(active_app);
    data[7] = (uint8_t)(active_app >> 8);
    data[8] = (uint8_t)(active_app >> 16);
    data[9] = (uint8_t)(active_app >> 24);
    data[10] = (uint8_t)((int)APP_VERSION);
    data[11] = (uint8_t)((int)((APP_VERSION - (int)APP_VERSION) * 10 + 0.5));
    data[12] = 0;

    send_packet(CMD_CHIP_INFO, data, PKT_DATA_LEN);
}

// ======================== Task main loop ========================
void UART_Query_Task(void *pvParameters)
{
    (void)pvParameters;
    LOG_I("[QUERY] Task started\r\n");

    uint8_t state = 0;  // 0=wait HEADER1, 1=wait HEADER2, 2=wait TYPE

    while (1)
    {
        // Burst read from interrupt ring buffer
        int ch;
        while ((ch = UART_RxGet()) >= 0) {
            switch (state) {
            case 0:
                if (ch == PKT_HEADER1) state = 1;
                break;
            case 1:
                state = (ch == PKT_HEADER2) ? 2 : 0;
                break;
            case 2:
                if (ch == CMD_CHIP_INFO) {
                    handle_chip_info_query();
                }
                state = 0;
                break;
            default:
                state = 0;
                break;
            }
        }

        vTaskDelay(pdMS_TO_TICKS(10));
    }
}
```

### 4.3 `bootloader/ota.c`（M2 + 真 AB）

```c
/**
  ******************************************************************************
  * @file    ota.c
  * @brief   OTA 升级 — 真 AB：YMODEM 写入【非活跃槽】-> CRC32 -> 翻转 active -> 重启
  *
  *          方案：A、B 两槽等大镜像。OTA 把新固件写入当前非活跃槽（不影响正在
  *          运行的活跃槽），校验通过后翻转 active_partition 并重启；bootloader
  *          下次启动跳到新槽。原活跃槽固件完整保留，作为回滚目标（见 boot_decision）。
  ******************************************************************************
  */

#include "ota.h"
#include "boot_config.h"
#include "boot_decision.h"
#include "boot_jump.h"
#include "ota_params.h"
#include "flash_control.h"
#include "ymodem.h"
#include "Delay.h"
#include <stdio.h>
#include <ctype.h>

// g_ota_param 在 boot_main.c 中定义
extern ota_param_t g_ota_param;

// ===== 从 YMODEM 文件名解析版本号 (M2) =====
// 约定文件名形如 "xxx_v1_2.bin" / "xxx_v1.2.bin"（v 不区分大小写）。
// 解析成功返回 (major<<16)|minor；失败返回 0（未知）。
static uint32_t parse_version_from_name(const char *name)
{
    if (name == NULL) {
        return 0;
    }

    for (const char *p = name; *p != '\0'; p++) {
        if (tolower((unsigned char)*p) != 'v') {
            continue;
        }
        const char *s = p + 1;
        if (!isdigit((unsigned char)*s)) {
            continue;
        }

        uint32_t maj = 0;
        while (isdigit((unsigned char)*s)) {
            maj = maj * 10 + (uint32_t)(*s - '0');
            s++;
        }
        if (*s != '.' && *s != '_') {
            continue;
        }
        s++;

        uint32_t min = 0;
        if (!isdigit((unsigned char)*s)) {
            continue;
        }
        while (isdigit((unsigned char)*s)) {
            min = min * 10 + (uint32_t)(*s - '0');
            s++;
        }
        return (maj << 16) | (min & 0xFFFF);
    }
    return 0;
}

// ===== YMODEM 写入非活跃槽 + 校验 + 翻转 active + 重启 =====
void ota_ymodem_start(void)
{
    // 目标 = 当前非活跃槽（写入它不影响正在运行的活跃槽）
    uint8_t  target_part = (g_ota_param.active_partition == APP_A_ACTIVE)
                           ? APP_B_ACTIVE : APP_A_ACTIVE;
    uint32_t target_addr = (target_part == APP_A_ACTIVE)
                           ? APP_A_ADDR : APP_B_ADDR;
    uint32_t target_size = (target_part == APP_A_ACTIVE)
                           ? APP_A_SIZE : APP_B_SIZE;

    printf("[BOOT] Starting YMODEM OTA...\r\n");
    printf("[BOOT] Active=%s, target(inactive)=%s @ 0x%08X (%uKB)\r\n",
           g_ota_param.active_partition == APP_A_ACTIVE ? "App A" : "App B",
           target_part == APP_A_ACTIVE ? "App A" : "App B",
           (unsigned int)target_addr,
           (unsigned int)(target_size / 1024));

    ymodem_status_t status;
    int ret = ymodem_receive(target_addr, target_size, &status);

    if (ret == YMODEM_OK) {
        printf("[BOOT] YMODEM transfer OK, verifying...\r\n");

        // 校验镜像链接地址是否匹配目标槽（防错包）：
        // 位置链接的镜像，其向量表 reset-handler(偏移 +4) 必然落在自身槽地址范围内。
        // 若落在另一槽范围，说明上位机发错了 bin（如活跃 A 时发了 app.bin 而非 app_b.bin）。
        uint32_t reset_handler = *(volatile uint32_t *)(target_addr + 4);
        if (reset_handler < target_addr ||
            reset_handler >= target_addr + target_size) {
            printf("[BOOT] ERROR: image reset-handler 0x%08X not in target slot "
                   "[0x%08X-0x%08X) - wrong bin for this slot! Aborting (active unchanged).\r\n",
                   (unsigned int)reset_handler,
                   (unsigned int)target_addr,
                   (unsigned int)(target_addr + target_size));
            return;   /* 不翻转 active、不重启；活跃槽不受影响，回到重试循环 */
        }

        uint32_t crc = crc32_flash(target_addr, status.total_received);
        uint32_t ver = parse_version_from_name((const char *)status.file_name);
        printf("[BOOT] CRC32: 0x%08X, version: 0x%08X (from '%s')\r\n",
               (unsigned int)crc, (unsigned int)ver,
               (const char *)status.file_name);

        // 记录新槽元数据（旧槽元数据保持不变，作为回滚依据）
        if (target_part == APP_A_ACTIVE) {
            g_ota_param.app_a_version = ver;
            g_ota_param.app_a_size    = status.total_received;
            g_ota_param.app_a_crc32   = crc;
        } else {
            g_ota_param.app_b_version = ver;
            g_ota_param.app_b_size    = status.total_received;
            g_ota_param.app_b_crc32   = crc;
        }

        // 翻转 active 到新槽，标记 COMPLETE 待下次启动校验
        g_ota_param.active_partition = target_part;
        g_ota_param.ota_state        = OTA_STATE_COMPLETE;
        g_ota_param.boot_count       = 0;
        ota_params_save(&g_ota_param);

        printf("[BOOT] OTA params updated, active=%s. Rebooting...\r\n",
               target_part == APP_A_ACTIVE ? "App A" : "App B");
        Delay_ms(500);
        NVIC_SystemReset();
    } else {
        printf("[BOOT] YMODEM failed (code: %d). Active slot untouched.\r\n", ret);
    }
}
```

### 4.4 `mdk/app_b.sct`（[新建]）

```
; *************************************************************
; *** Scatter-Loading Description File — App B (真 AB 的 B 槽) ***
; *** STM32F429IGTx App B 384KB (0x08080000-0x080DFFFF)       ***
; *************************************************************
; 与 app.sct 仅加载/执行区地址不同；RAM 布局相同（A/B 任一时刻只运行一份）。

LR_IROM1 0x08080000 0x00060000  {    ; 加载区：App B 384KB
  ER_IROM1 0x08080000 0x00060000  {  ; 执行区
   *.o (RESET, +First)
   startup_*.o (+RO)
   .ANY (+RO)
  }
  RW_IRAM1 0x20000000 0x00020000  {  ; 128KB 主 SRAM (SRAM1 112KB + SRAM2 16KB)；外设/DMA 缓冲与一般数据
   .ANY (+RW +ZI)
  }
  ; ---- CCM 64KB @0x10000000：仅 CPU 可访问，DMA 不可达 ----
  ; ---- 放 FreeRTOS 堆 (heap_4 的 ucHeap 64KB)，释放主 SRAM；DMA 缓冲严禁经 heap 分配至此 ----
  RW_IRAM2 0x10000000 0x00010000  {
   heap_4.o (+ZI)
  }
}
```

### 4.5 `bootloader/boot_decision.c`（M1 + 真 AB）

```c
/**
  ******************************************************************************
  * @file    boot_decision.c
  * @brief   启动决策状态机 — 真 AB：按 active_partition 选址 / 切槽回滚 / 进入 OTA
  *
  *          真 AB 方案：A、B 两槽各存一份完整镜像，bootloader 按 active_partition
  *          跳转活跃槽。新固件 OTA 写入【非活跃槽】并翻转 active；旧固件仍留在
  *          原活跃槽，作为回滚目标。故回滚 = 切回另一槽，无需 384KB Flash 搬运。
  ******************************************************************************
  */

#include "boot_decision.h"
#include "boot_config.h"
#include "boot_jump.h"
#include "ota_params.h"
#include "flash_control.h"
#include <stdio.h>

// g_ota_param 在 boot_main.c 中定义
extern ota_param_t g_ota_param;

// ===== 当前活跃槽地址（按 active_partition 选择） =====
uint32_t get_active_addr(void)
{
    return (g_ota_param.active_partition == APP_A_ACTIVE) ? APP_A_ADDR : APP_B_ADDR;
}

// ===== 当前活跃槽的元数据 =====
static uint32_t active_size(void)
{
    return (g_ota_param.active_partition == APP_A_ACTIVE)
           ? g_ota_param.app_a_size : g_ota_param.app_b_size;
}
static uint32_t active_crc(void)
{
    return (g_ota_param.active_partition == APP_A_ACTIVE)
           ? g_ota_param.app_a_crc32 : g_ota_param.app_b_crc32;
}

// ===== 回滚：切到另一槽（真 AB 无需 Flash 搬运） =====
static int rollback_to_other(void)
{
    uint8_t  other  = (g_ota_param.active_partition == APP_A_ACTIVE)
                      ? APP_B_ACTIVE : APP_A_ACTIVE;
    uint32_t o_addr = (other == APP_A_ACTIVE) ? APP_A_ADDR : APP_B_ADDR;
    uint32_t o_size = (other == APP_A_ACTIVE)
                      ? g_ota_param.app_a_size : g_ota_param.app_b_size;
    uint32_t o_crc  = (other == APP_A_ACTIVE)
                      ? g_ota_param.app_a_crc32 : g_ota_param.app_b_crc32;

    printf("[BOOT] Rollback candidate: %s.\r\n",
           other == APP_A_ACTIVE ? "App A" : "App B");

    if (!partition_is_valid(o_addr)) {
        printf("[BOOT] Other slot SP invalid, cannot roll back.\r\n");
        return -1;
    }

    if (o_size == 0 || o_size == 0xFFFFFFFF) {
        // 另一槽无元数据（如出厂首烧的槽）-> 仅凭 SP 合法性信任
        printf("[BOOT] Other slot has no metadata, trust by SP.\r\n");
    } else {
        uint32_t calc = crc32_flash(o_addr, o_size);
        if (calc != o_crc) {
            printf("[BOOT] Other slot CRC mismatch (0x%08X vs 0x%08X).\r\n",
                   (unsigned int)calc, (unsigned int)o_crc);
            return -1;
        }
    }

    g_ota_param.active_partition = other;
    printf("[BOOT] Rollback to %s.\r\n",
           other == APP_A_ACTIVE ? "App A" : "App B");
    return 0;
}

// ===== 启动决策状态机 =====
int boot_decision(void)
{
    switch (g_ota_param.ota_state) {

    case OTA_STATE_IDLE:
        if (partition_is_valid(get_active_addr())) {
            return 1;
        }
        printf("[BOOT] Active slot invalid.\r\n");
        return 0;

    case OTA_STATE_COMPLETE:
        {
            g_ota_param.boot_count++;
            ota_params_save(&g_ota_param);

            printf("[BOOT] Boot attempt %u/%u\r\n",
                   g_ota_param.boot_count,
                   g_ota_param.max_boot_count);

            uint32_t addr = get_active_addr();
            uint32_t size = active_size();
            uint32_t crc  = active_crc();

            if (size == 0 || size == 0xFFFFFFFF) {
                printf("[BOOT] Active slot has no metadata, entering OTA.\r\n");
                return 0;
            }

            uint32_t calc_crc = crc32_flash(addr, size);
            printf("[BOOT] CRC32: saved=0x%08X calc=0x%08X\r\n",
                   (unsigned int)crc, (unsigned int)calc_crc);

            if (calc_crc == crc) {
                printf("[BOOT] Firmware verified OK.\r\n");
                g_ota_param.ota_state  = OTA_STATE_IDLE;
                g_ota_param.boot_count = 0;
                ota_params_save(&g_ota_param);
                return 1;
            }

            // CRC 失败，检查是否超限
            if (g_ota_param.boot_count >= g_ota_param.max_boot_count) {
                printf("[BOOT] Max boot attempts, rolling back to other slot...\r\n");
                if (rollback_to_other() == 0) {
                    g_ota_param.ota_state  = OTA_STATE_IDLE;
                    g_ota_param.boot_count = 0;
                    ota_params_save(&g_ota_param);
                    return 1;
                }
                printf("[BOOT] Rollback failed, entering OTA mode.\r\n");
                return 0;
            }

            // 未超限：重启重试（boot_count 已持久化，重启后递增；超限则切槽回滚）
            printf("[BOOT] CRC mismatch (attempt %u/%u), reboot to retry...\r\n",
                   g_ota_param.boot_count, g_ota_param.max_boot_count);
            NVIC_SystemReset();
            return 0;   /* 不可达，保险 */
        }

    case OTA_STATE_FAILED:
        printf("[BOOT] Previous upgrade failed, entering OTA mode.\r\n");
        return 0;

    default:
        printf("[BOOT] Unknown OTA state %u, entering upgrade mode.\r\n",
               g_ota_param.ota_state);
        return 0;
    }
}
```

### 4.6 `bootloader/ota.h`

```c
/**
  ******************************************************************************
  * @file    ota.h
  * @brief   OTA 升级 — 真 AB：YMODEM 写非活跃槽 / 校验 / 翻转 active / 重启
  ******************************************************************************
  */

#ifndef __OTA_H
#define __OTA_H

void ota_ymodem_start(void);   /* 写非活跃槽 + 校验 + 翻转 active + 重启 */

#endif /* __OTA_H */
```

---

## 5. 复现校验（应用全部改动后）

1. 编译三目标（命令行；UV4 路径按本机调整）：
   - `UV4 -r mdk/boot.uvprojx -o build_boot.log` → 期望 **0E 0W**
   - `UV4 -r mdk/app.uvprojx -t stm32f429 -o build_app.log` → 期望 **0E 1W**（port.c #550-D）
   - `UV4 -r mdk/app.uvprojx -t stm32f429_b -o build_app_b.log` → 期望 **0E 1W**（port.c #550-D）
2. map 核对（`mdk/Listings/*.map`）：
   - `app.map` 含 `Execution Region ER_IROM1 (Exec base: 0x08020000, ...)`
   - `app_b.map` 含 `Execution Region ER_IROM1 (Exec base: 0x08080000, ...)`
   - `boot.map` 含 `Execution Region ER_IROM1 (Exec base: 0x08000000, ...)`
3. 产物独立：`Objects\boot.axf`、`Objects\app.axf`(+`app.bin`)、`Objects\app_b.axf`(+`app_b.bin`)。
4. 行为不变量：CAN 收发、YMODEM、按键、串口协议逻辑等对外行为与基线一致；OTA 升级语义升级为"真 AB（写非活跃槽、瞬时切换、切槽回滚）"。

---

## 6. 应用顺序建议

按文件分组应用（同文件多处按本文档"修改前/后"逐条替换）：
1. `bootloader/ota_params.c`（A-H1/A-L5：整文件替换为 4.1）。
2. `app/bsp_can.c`（A-M5）、`task/mod_comm_can.h`（A-L1）、`app/bsp_key.c`（A-L4）。
3. `task/task_query.c`（A-M3/M4：整文件替换为 4.2）、`mdk/app.uvprojx`（A-L2：OutputName + IncludePath）、`mdk/boot.uvprojx`（A-L2：OutputName）。**此步后可先编 boot+app 验证阶段 A 绿**。
4. `firmware/cmsis/device/system_stm32f4xx.c`（3.1：APP_SLOT_B 分支）。
5. 新建 `mdk/app_b.sct`（4.4）；在 `mdk/app.uvprojx` 追加第二 Target "stm32f429_b"（3.6，**不再创建独立的 app_b.uvprojx**）。
6. `bootloader/boot_decision.c`（3.3：整文件替换为 4.5）、`bootloader/ota.c`（3.4：整文件替换为 4.3）、`bootloader/ota.h`（4.6）、`bootloader/boot_main.c`（3.5：移除备份调用）。
7. 三目标分别编译，按第 5 节核对。

> 缩进提醒：`driver/usart.c`、`app/main.c`、`task/task_entry.c`、`app/bsp_key.c`、`task/mod_comm_can.c` 部分行用 **Tab**；`bootloader/*`、散列文件、头文件多用空格。替换时逐字保持。
